fails:Regexp.compile works by default for subclasses with overridden #initialize
fails:Regexp.compile given a String enables EUC encoding if third argument is 'e' or 'euc' (case-insensitive)
fails:Regexp.compile given a String enables SJIS encoding if third argument is 's' or 'sjis' (case-insensitive)
fails:Regexp.compile given a String enables UTF-8 encoding if third argument is 'u' or 'utf8' (case-insensitive)
fails:Regexp.compile given a String disables multibyte support if third argument is 'n' or 'none' (case insensitive)
fails:Regexp.compile given a Regexp does not honour options given as additional arguments
fail:Regexp.compile works by default for subclasses with overridden #initialize
fail:Regexp.compile given a String enables EUC encoding if third argument is 'e' or 'euc' (case-insensitive)
fail:Regexp.compile given a String enables SJIS encoding if third argument is 's' or 'sjis' (case-insensitive)
fail:Regexp.compile given a String enables UTF-8 encoding if third argument is 'u' or 'utf8' (case-insensitive)
fail:Regexp.compile given a String disables multibyte support if third argument is 'n' or 'none' (case insensitive)
fail:Regexp.compile given a Regexp does not honour options given as additional arguments
fails:Range.compile works by default for subclasses with overridden #initialize

critical:ARGF.gets reads one line of a file
critical:ARGF.gets reads all lines of a file
critical:ARGF.gets reads all lines of two files
critical:ARGF.gets returns nil when reaching end of files
critical:ARGF.gets sets $_ global variable with each line read
fails:ARGF.gets reads all lines of stdin
